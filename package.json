{
  "name": "gas-workflow-javascript",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/main.js",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "node esbuild.js",
    "pull": "clasp pull",
    "push": "clasp push",
    "open": "clasp open-script",
    "release": "npm run build && npm run push",
    "format": "prettier --write 'src/**'"
  },
  "devDependencies": {
    "@google/clasp": "^3.1.3",
    "esbuild": "^0.27.0",
    "esbuild-gas-plugin": "^0.10.0",
    "prettier": "^3.7.2"
  }
}